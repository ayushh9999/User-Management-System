
<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Basic meta tags for proper rendering and character encoding -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User</title>
    <!-- Link to custom stylesheet for additional styling -->
    <link rel="stylesheet" href="stylesheets/style.css">
    <!-- Include Tailwind CSS framework via CDN for utility classes -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <!-- Main container with dark theme and padding -->
    <div class="w-full h-full bg-zinc-800 p-10 text-white">
        <!-- Navigation section with link back to users list -->
        <div class="nav mb-20">
            <a class="text-blue-500 hover:text-blue-400" href="/read">Read Users</a>
        </div>
        <!-- Page heading with custom tracking -->
        <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
        <!-- User update form - Posts to /update/:id endpoint -->
        <form action="/update/<%= user._id %>" method="post">
            <!-- Name input field with existing value -->
            <input 
                class="px-5 py-3 bg-transparent border-2 border-zinc-800 focus:border-zinc-700 outline-none" 
                type="text" 
                name="name" 
                placeholder="name" 
                value="<%= user.name %>"
                required
            >
            <!-- Email input field with existing value -->
            <input 
                class="px-5 py-3 bg-transparent border-2 border-zinc-800 focus:border-zinc-700 outline-none" 
                type="email" 
                name="email" 
                placeholder="email" 
                value="<%= user.email %>"
                required
            >
            <!-- Image URL input field with existing value -->
            <input 
                class="px-5 py-3 bg-transparent border-2 border-zinc-800 focus:border-zinc-700 outline-none" 
                type="url" 
                name="image" 
                placeholder="image url" 
                value="<%= user.image %>"
                required
            >
            <!-- Submit button with yellow background -->
            <input 
                class="px-5 py-3 bg-yellow-500 hover:bg-yellow-600 text-black rounded-lg ml-3 cursor-pointer" 
                type="submit" 
                value="Update User"
            >
        </form>
    </div>
</body>
</html>